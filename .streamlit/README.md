# Streamlit Configuration

Contains configuration for the SecureFlow demo dashboard deployed on Streamlit Cloud.

## Files

### config.toml
Streamlit configuration file that defines:
- Theme settings (colors, fonts, layout)
- Server configuration
- Authentication and security settings
- Performance and caching options

This configures the appearance and behavior of the Streamlit dashboard at `app.py`.
