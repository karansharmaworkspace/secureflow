# Java Package Root

Root of the Java package hierarchy for the Spring Boot banking API. Package structure:

```
java/
└── com/
    └── unionbank/
        └── api/
            ├── LoanController.java     (v3 active — 4 routes)
            └── ZombieController.java   (v1 zombie — 5 routes)
```
