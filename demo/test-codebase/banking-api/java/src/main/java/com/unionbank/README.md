# unionbank Package

Union Bank Java package namespace. Contains the `api` sub-package with 2 Spring Boot controllers:

- `LoanController.java` — 4 active v3 loan endpoints (status, schedule, prepayment, documents)
- `ZombieController.java` — 5 zombie v1 admin endpoints (users, config, temp, mock)
