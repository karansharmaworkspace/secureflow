# Python Banking API

Python implementation of the simulated banking API using Flask/FastAPI-style structure. Contains models, route handlers, and legacy API versions for comprehensive test coverage.

## Directory Structure

### models/
Python data models representing banking entities (users, accounts, transactions).

### routes/
Route handler modules for accounts, authentication, and payments endpoints. Includes a legacy v1 API directory with outdated endpoints representing zombie candidates.
