# Python Models

Data model definitions for the Python banking API implementation. Defines entity schemas for users, accounts, transactions, and other banking domain objects used across route handlers.
