# Java Source Root

Root of the Java package hierarchy. The package structure follows standard Java conventions: the full source path is `java/com/logiclegion/zombie/`. The `com` → `logiclegion` → `zombie` hierarchy represents the organization (Logic Legion) and the project module (zombie detection).

Source layout:
```
java/
└── com/
    └── logiclegion/
        └── zombie/
            ├── ApiCallRecord.java
            ├── EndpointFeatures.java
            ├── FeatureComputationJob.java
            └── SyntheticTrafficDetector.java
```
