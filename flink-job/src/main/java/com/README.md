# com Package

Top-level organization package namespace. `com.logiclegion.zombie` is the fully qualified package for the Flink zombie detection job. Contains all 4 Java source files that form the streaming feature computation pipeline.
