# logiclegion Test Package

Test package for Logic Legion's Flink job. Contains the `zombie` sub-package with `FeatureAggregatorTest.java` — unit tests for the custom Flink aggregate function that computes endpoint features from raw API call records.
