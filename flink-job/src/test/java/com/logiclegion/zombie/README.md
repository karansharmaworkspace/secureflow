# Zombie Detection Flink Job Tests

Unit and integration tests for the Flink job components. Validates feature computation logic, Kafka integration, and windowing operations for the zombie endpoint detection pipeline.
