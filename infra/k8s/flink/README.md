# Flink - Stream Processing

Apache Flink operator and deployment configuration for real-time feature computation. Consumes traffic data from Kafka and computes rolling endpoint features for ML classification.

## Configuration

- Flink operator for Kubernetes-native deployment
- Kafka topic subscriptions for data ingestion
- Windowed feature computation jobs
- State backend configuration for checkpointing
