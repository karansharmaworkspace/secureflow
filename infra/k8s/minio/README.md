# MinIO - Object Storage

S3-compatible object storage for the SecureFlow platform. Stores feature snapshots, ML model artifacts, and compliance audit trails.

## Configuration

- Multi-bucket setup for different data types
- Persistent volume storage
- Console UI for management
- S3 API compatibility for integration with MLflow and Feast
